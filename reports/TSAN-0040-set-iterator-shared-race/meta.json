{
  "id": "TSAN-0040",
  "slug": "set-iterator-shared-race",
  "title": "set: setiter_iternext advances a shared set iterator's cursor (si_used/len/index) non-atomically vs setiter_len's read (Objects/setobject.c)",
  "description": "The set iterator (setiterobject in Objects/setobject.c, returned by iter(set)/iter(frozenset)) keeps its position in plain fields (si_used snapshot + len countdown + internal table index) with no synchronization. When a single set iterator is shared across threads, setiter_iternext (setobject.c:~1093) advances that state while setiter_len (the __length_hint__ slot, setobject.c:1063) plainly reads it -> data race on the cursor. This is the set form of the same builtin-iterator shared-cursor defect as the str (cpython#153928 / TSAN-0038), bytes (TSAN-0037), struct (cpython#154013 / TSAN-0039), and dict (TSAN-0026) iterators. Surfaced by fusil's --tsan shared-iterator op (PR #211/#213): op h shares one iter(set(range(4096))) by reference, some workers advancing it via next() and some reading its cursor via operator.length_hint.",
  "signatures": [
    "Objects/setobject.c:setiter_iternext | Objects/setobject.c:setiter_len",
    "Objects/setobject.c:setiter_iternext | Objects/setobject.c:setiter_iternext"
  ],
  "sites": [
    "Objects/setobject.c setiter_iternext:~1093 (WRITE/advance the shared cursor: si->len countdown and the table scan index; the non-atomic advance the fleet's next() drives)",
    "Objects/setobject.c setiter_len:1063 (READ si->len for __length_hint__, driven by the fleet's operator.length_hint reader)"
  ],
  "race_kind": "shared-iterator state: non-atomic cursor read/write on a single shared set iterator -> data race. A builtin C iterator's per-iteration state mutated with no per-object critical section (same class as TSAN-0037 bytes / TSAN-0038 str / TSAN-0039 struct / TSAN-0026 dict).",
  "shared_state": "setiterobject fields (si_used snapshot, len countdown, internal hash-table scan index, and its reference to the source set) on a single shared iterator",
  "severity": "moderate (a data race on the cursor -> mis-stepped / duplicated / skipped elements, or a stale si_used consistency check, under concurrency). Sharing one iterator across threads is unusual (lower real-world priority), but setobject declares no per-iterator locking.",
  "reproduced": true,
  "repro": "repro.py",
  "repro_reliability": "deterministic: repro.py (8 threads, half advancing one shared iter(set(range(4096))) via next(), half reading its cursor via operator.length_hint) exits 66 on the debug-ft-nojit-tsan build; observed the setiter_iternext self-race face on the isolated repro. Fleet-10 drove both the setiter_len face (6 veh) via the length_hint reader.",
  "suggested_fix": "Same as the str/bytes/struct iterators: make the set-iterator's countdown/index an atomic load/store, or take a per-iterator critical section over setiter_iternext / setiter_len. The whole builtin sequence/collection-iterator family shares this shape.",
  "status": "drafted",
  "upstream_issue": null,
  "found_date": "2026-07-18",
  "found_in": "fusil-tsan_fleet_10",
  "vehicles": 6,
  "vehicle_examples": [
    "inst-01/copy-warning_threadsanitizer_data_race-tsanNEW",
    "inst-01/ntpath-warning_threadsanitizer_data_race-tsanNEW",
    "inst-02/pprint-warning_threadsanitizer_data_race-tsanNEW"
  ],
  "confirmed_build": "debug-ft-nojit-tsan (CPython 3.16.0a0, --disable-gil --with-thread-sanitizer); also reproduced on release-ft-nojit-tsan (release + TSan) -> NOT a debug-only artifact",
  "confirmed_unfixed_on": "3.16.0a0 (fleet build main@a1d580430c8, predates open PR #144357)",
  "status": "reported",
  "upstream_issue": 144356,
  "upstream_pr": 144357,
  "related_issues": ["cpython#144356", "cpython#144357", "cpython#124397", "cpython#112069", "cpython#153928", "cpython#154013"],
  "prior_art": "ALREADY REPORTED = gh-144356 ('Data race in set iterator length_hint under no-gil', OPEN), fix in OPEN PR #144357 ('gh-144356: Make set iterator __length_hint__ and iternext race-safe under no-gil'). PR #144357's diff fixes EXACTLY this race: setiter_len now runs under Py_BEGIN_CRITICAL_SECTION(op) and setiter_iternext under Py_BEGIN_CRITICAL_SECTION2(self, so) with `si->si_pos = i+1; si->len--;` MOVED INSIDE the section. Background: gh-112069/PR #117935 (MERGED 2024) earlier hardened setiter_iternext's access to the SET (critical section on `so`) but left the per-ITERATOR cursor plain -- #144356/#144357 is the follow-up that covers the iterator's own si->len/si_pos. Found via gh api search (web search missed #144356).",
  "related": "Same builtin-iterator shared-cursor class as TSAN-0037 (bytes striter_next), TSAN-0038 (str unicode iter / #153928), TSAN-0039 (struct unpackiter / #154013), and TSAN-0026 (dict iterator). The op-h iterator pool (P3.1) is what shares these. CPython's iterator-FT strategy is gh-124397 (rhettinger): C iterators get only minimal 'don't crash' changes -- concurrent access may return duplicate/skipped values or raise. This set race is VALUE-BENIGN (the element fetch is under the set CS; only si->len / length_hint races), so per that strategy it is low-priority -- but the maintainers chose to make it fully race-clean anyway (#144356/#144357).",
  "notes": "NOT a new find: this is gh-144356, fix pending in OPEN PR #144357 (which covers exactly the si->len / si_pos cursor via Py_BEGIN_CRITICAL_SECTION2(self, so)). Independently reproduced by fusil (fleet-10, 6 veh) on debug AND release TSan builds (exit 66). No new filing; a confirmation on #144356 that fusil reproduces it is the only outward-facing step, at the maintainer's discretion. Re-run repro.py to move status -> fixed when #144357 merges."
}
